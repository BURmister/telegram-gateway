services:
  telegram-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    image: telegram-gateway:latest
    container_name: telegram-gateway
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: ${PORT:-4200}
    ports:
      - "127.0.0.1:${PORT:-4200}:${PORT:-4200}"
